# Refactor handoff

Current stage: P4c generation/evaluation code gate complete; P4d CLI/config/tools is next. Isolated local branch `refactor/flat-layout` starts at source HEAD `d8f674aad692c8426cf9240d07b3384f7a043888`. All P1–P3 and P4a–P4c runtime files are in flat `molvid/`; no legacy runner or file has been retired. The old source worktree remains read-only. See `docs/migration.md` for exact parity evidence.

Environment: interactive `enter-container` works; activate `torch-ito` there. Old-path P0 CUDA suite: 60 passed; combined P1–P4c2 CUDA suite: 97 passed. P4c3 evaluator/report tests: 5 passed, including exact old/new forecast metrics and generated-prefix isolation. Four real codec controls and the historical 166-moment DiT baseline passed strict state migration gates. Short observed-only rollout passed on tiny CUDA models; full historical codec+DiT same-input generation and real-system rollout remain `not_run`. Original artifacts remain untouched. Historical codec RNG and DiT data/sampler-dependent continuation remain `not_run`; old runners stay live.
