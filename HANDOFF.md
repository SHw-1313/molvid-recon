# Refactor handoff

Current stage: P4b DiT trainer and historical optimizer gate complete; P4c generation/evaluation is next. Isolated local branch `refactor/flat-layout` starts at source HEAD `d8f674aad692c8426cf9240d07b3384f7a043888`. All P1–P3 and P4a–P4b runtime files are in flat `molvid/`; no legacy runner or file has been retired. The old source worktree remains read-only. See `docs/migration.md` for exact parity evidence.

Environment: interactive `enter-container` works; activate `torch-ito` there. Old-path P0 CUDA suite: 60 passed; combined P1–P4b CUDA suite: 82 passed. Four real codec controls migrated with exact source hashes, all weights and AdamW moments; R4 old/new forward, gradients and one update matched under deterministic CUDA. Historical DiT baseline transferred 166 weights and AdamW moments with exact fixed-batch next update; new-format resume matched next RNG draws and update. Original artifacts remain untouched. Next: P4c observed-only generation and evaluation. Historical codec RNG and historical DiT data/sampler-dependent continuation remain `not_run`; old runners stay live.
