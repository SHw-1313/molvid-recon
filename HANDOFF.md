# Refactor handoff

Current stage: P5 diagnostic checkpoint with numerical sign-off withheld. Isolated local branch `refactor/flat-layout` starts at source HEAD `d8f674aad692c8426cf9240d07b3384f7a043888`. All 50 planned runtime files are in flat `molvid/`; no legacy runner or file has been retired. The old source worktree remains read-only. See `docs/migration.md` for parity evidence and the exact failing gate.

Environment: interactive `enter-container` works; activate `torch-ito` there. Old-path P0 CUDA suite: 60 passed; retained new-path suite: 109 passed after the P5 generation change. The separate real-clip zero-tolerance CUDA gate fails at a 910-atom temporal linear layer despite equal inputs/weights/bias; final old/new coordinates differ at most 7.63e-6 Å. The 166-moment historical DiT migration and tiny-path resume/future-isolation gates passed. Real rollout, old-runner metrics, historical sampler continuation, stage-wise profile and raw preprocessing remain `not_run`; no old paths are safe to retire.
