# Refactor handoff

Current stage: P5 single-H8 and expanded H4/two-segment H8 real-weight numerical gates passed at separate approved coordinate bounds (1e-5 Å and 3e-5 Å absolute respectively, rtol 0). Overall production cutover is withheld. Isolated local branch `refactor/flat-layout` starts at source HEAD `d8f674aad692c8426cf9240d07b3384f7a043888`. All 50 planned runtime files are in flat `molvid/`; four obsolete historical test files were retired, but no old production runner or source file has been retired. The old source worktree remains read-only. See `docs/migration.md` for evidence and remaining gates.

Environment: interactive `enter-container` works; activate `torch-ito` there. Old-path P0 CUDA suite: 60 passed; the full retained new-path suite is 116 passed on CUDA GPU 5. The original single-H8 gate stays at coordinate atol 1e-5 Å and latent atol 1e-4, rtol 0; H4/two-segment H8 use the separate user-approved coordinate atol 3e-5 Å, rtol 0. Exact weight/prefix checks are unchanged. Historical capacity C48 parent train48 sampler schedule/cursor/next three batches match old/new exactly, but the later baseline checkpoint names another data hash, so direct historical training continuation on that manifest is unverified. Read-only dataset/model inspectors and a cold single-clip CUDA stage profile passed; the profile is not an old/new performance comparison. Four history-only temporal/anchor/ViSNet tests were removed after reachability audit; old round and contract tests remain where runners still reference them. Old-runner metrics, full training continuation, raw preprocessing, artifact export and a persistent new-format DiT inspector CLI run remain `not_run`; no old production paths are safe to retire.
