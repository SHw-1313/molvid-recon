# Refactor handoff

Current stage: P5 real-clip numerical gate accepted at bounded tolerance; overall cutover and retirement withheld. Isolated local branch `refactor/flat-layout` starts at source HEAD `d8f674aad692c8426cf9240d07b3384f7a043888`. All 50 planned runtime files are in flat `molvid/`; no legacy runner or file has been retired. The old source worktree remains read-only. See `docs/migration.md` for exact parity evidence and remaining gates.

Environment: interactive `enter-container` works; activate `torch-ito` there. Old-path P0 CUDA suite: 60 passed; retained new-path suite: 110 passed including the historical full-weight real H8 clip. The user accepted bounded floating differences: latent absolute tolerance 1e-4 (observed up to 4.58e-5) and final coordinate tolerance 1e-5 Å (previously observed up to 7.63e-6 Å), both with zero relative tolerance. Inputs, topology, weights and observed prefix remain exact. The 166-moment historical DiT migration and tiny-path resume/future-isolation gates passed. Read-only dataset and model inspectors were added; real codec and historical-DiT-forward checks passed. Real H4/H8 old-runner rollout/metrics, historical sampler continuation, stage-wise profile, raw preprocessing and a persistent new-format DiT inspector CLI run remain `not_run`; no old paths are safe to retire.
