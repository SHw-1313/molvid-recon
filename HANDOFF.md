# Refactor handoff

Current stage: P3b DiT extraction complete; P3 flow and full inference isolation are next. Isolated local branch `refactor/flat-layout` starts at source HEAD `d8f674aad692c8426cf9240d07b3384f7a043888`. All P1/P2 and current P3a/P3b runtime files are in flat `molvid/`; no legacy runner or file has been retired. The old source worktree remains read-only. See `docs/migration.md` for exact parity evidence.

Environment: interactive `enter-container` works; activate `torch-ito` there. Old-path P0 CUDA suite: 60 passed; combined P1–P3b CUDA suite: 62 passed. Four real codec controls migrated with exact source hashes, all weights and AdamW moments; R4 old/new forward, gradients and one update matched under deterministic CUDA. New-format resume matched next RNG draws and update. Original artifacts remain untouched. Next: P3 flow parity and full DiT inference-isolation gate. Historical codec checkpoint lacks RNG, so its bitwise old-format continuation is not claimed and the old trainer stays live.
