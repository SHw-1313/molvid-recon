# Refactor handoff

Current stage: P4d2 codec training CLI complete; DiT train/sample/evaluation CLI and P5 remain. Isolated local branch `refactor/flat-layout` starts at source HEAD `d8f674aad692c8426cf9240d07b3384f7a043888`. All P1–P3, P4a–P4c, preprocess and codec training runtime files are in flat `molvid/`; no legacy runner or file has been retired. The old source worktree remains read-only. See `docs/migration.md` for exact parity evidence.

Environment: interactive `enter-container` works; activate `torch-ito` there. Old-path P0 CUDA suite: 60 passed; combined P1–P4c CUDA suite: 102 passed. P4d2 training/CLI tests: 10 passed, including one CUDA update and SHA-checked resume. Four real codec controls and the historical 166-moment DiT baseline passed strict state migration gates; short observed-only rollout and old/new metrics passed on tiny CUDA clips. Full historical codec+DiT same-input generation, real-system rollout, raw preprocessing and historical data/sampler continuation remain `not_run`. Original artifacts remain untouched; old runners stay live.
