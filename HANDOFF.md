# Refactor handoff

Current stage: P4c1 observed-only generation is complete; P4c metrics/evaluation is next. Isolated local branch `refactor/flat-layout` starts at source HEAD `d8f674aad692c8426cf9240d07b3384f7a043888`. All P1–P3, P4a–P4b and generation runtime files are in flat `molvid/`; no legacy runner or file has been retired. The old source worktree remains read-only. See `docs/migration.md` for exact parity evidence.

Environment: interactive `enter-container` works; activate `torch-ito` there. Old-path P0 CUDA suite: 60 passed; combined P1–P4b CUDA suite: 82 passed. P4c1 generation tests: 3 passed. Four real codec controls and the historical 166-moment DiT baseline passed strict state migration gates. Short observed-only rollout passed on tiny CUDA models; reference-checkpoint full generation and metric extraction remain pending. Original artifacts remain untouched. Historical codec RNG and historical DiT data/sampler-dependent continuation remain `not_run`; old runners stay live.
