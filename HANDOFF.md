# Refactor handoff

Current stage: P5 single-clip H8 numerical gate accepted at bounded tolerance; an expanded H4/two-segment H8 gate is pending separate acceptance. Overall cutover and retirement withheld. Isolated local branch `refactor/flat-layout` starts at source HEAD `d8f674aad692c8426cf9240d07b3384f7a043888`. All 50 planned runtime files are in flat `molvid/`; no legacy runner or file has been retired. The old source worktree remains read-only. See `docs/migration.md` for exact evidence and remaining gates.

Environment: interactive `enter-container` works; activate `torch-ito` there. Old-path P0 CUDA suite: 60 passed; the retained new-path suite had 110 passed before the expanded gate was added. The user accepted single-H8 bounded floating differences: latent absolute tolerance 1e-4 and final coordinate tolerance 1e-5 Å, with zero relative tolerance; exact weight/prefix checks are unchanged. Expanded H4 and two-segment H8 differences reached 2.10e-5 Å and 1.14e-5 Å respectively, so the current 1e-5 Å extension fails and the suite is not currently green. Historical capacity C48 parent train48 sampler schedule/cursor/next three batches match old/new exactly, but the later baseline checkpoint names another data hash, so direct historical training continuation on that manifest is unverified. Read-only dataset and model inspectors and a cold single-clip CUDA stage profile passed; the profile is not an old/new performance comparison. Old-runner metrics, full training continuation, raw preprocessing and a persistent new-format DiT inspector CLI run remain `not_run`; no old paths are safe to retire.
