# Migration record

## Archive history purge (2026-09-20)

At operator request, `git-filter-repo` rewrote all 34 commits and removed the
root archive directory from every local Git reference. The directory contained
838 tracked files and no untracked files. No nested archive directory existed.
The active tree retains eight flat-only tests and `results_archive/`; no `src/`
or `molvid/models/` was created. The two untracked design files were preserved.

No Python/CUDA tests were rerun in this history-only batch. Historical 116/116
predates the purge and is not a new pass. Before rewriting, all 388 bundled raw
result files passed their SHA-256 audit. After rewriting, `rev-list --objects
--all` reports zero archive-directory paths. A complete pre-rewrite recovery
bundle exists outside the repository in `/tmp`; it is not a Git reference.
Real-artifact production-cutover gaps remain open.

## P2a — spatial encoder sub-checkpoint (2026-09-18)

Status: spatial-only extraction complete; P2 codec, weight/optimizer migration and full P2 gate remain pending. No legacy entrypoint or file was switched or removed.

- Added `molvid/spatial/{ops,torchmd,encoder}.py` and package marker. `FrameEncoder` uses the P1 graph boundary and the same TorchMD call/constructor order, with no old bridge/cross-attention, ViSNet or checkpoint-loading branch. The unused old `OptimizedDistance` object had no parameters/buffers and consumed no RNG; its removal was checked against the active TorchMD state dictionary.
- Fixed-seed old/new TorchMD initialization: all 27 state keys, tensor bytes and subsequent CPU RNG state matched exactly. New `FrameEncoder` state names are the direct `spatial_encoder.* → backbone.*` map. In `enter-container` / `torch-ito` on CUDA GPU 5, `tests/test_codec.py` passed (1 test): identical graph edges and weights, frame scalar/vector tensors, coordinate gradient and every parameter gradient with `rtol=atol=0` under `CUBLAS_WORKSPACE_CONFIG=:4096:8` and PyTorch deterministic algorithms.
- Without deterministic algorithms, the unchanged old model differed from itself on a repeated vector forward by up to `1.1920928955078125e-07` due to CUDA reduction order; this was diagnosed rather than widening the test tolerance. Exact-zero parity passed after deterministic execution was enabled for the test.
- `not_run`: full Haar/codec latent, decoded coordinate, loss, checkpoint weight/optimizer and resume parity. Next: split state/detail primitives and compose `TrajectoryCodec`; migrate the checkpoint strictly and run the complete P2 gate before P3.

## P0 — 2026-09-18

Status: P0 source audit and fixed-input CUDA oracle complete. No production code was changed or retired. P1 may start; its own checks remain mandatory before P2.

### Checkout and boundaries

- The supplied `molvid_recon` directory initially contained only the two design artifacts and no Git repository. The local `refactor/flat-layout` checkout was created from the clean `exp/dit-architecture-sequential-v1` HEAD `d8f674aad692c8426cf9240d07b3384f7a043888`; the source worktree remains untouched. Both design artifacts are currently untracked in this checkout. The design commit happened to equal this selected HEAD; it was not used as a rollback instruction.
- Selection was based on the inventory's real source paths: this branch contains `dit_geometry_supervision.py`, `dit_history_corruption.py`, Round 4 rollout and `motion_metrics.py`; the neighboring `exp/dit-capacity-data-v1` HEAD `77e7e72498b29cdb7460a23e400ef2fe8ce52ea4` omits those paths. Source selection awaits any user correction.
- The old root `AGENTS.md` describes earlier experiment lanes. The current requested P0–P5 migration supersedes those lane-specific stops; the train/validation-only and sealed-test data boundary remains in force. No test clip payload was opened.

### Current runnable paths and actual source map

The active codec CLI is `train_codec.py` with `config/codec.yaml`; `scripts/run_state_detail_codec_v2_t1.py` owns the approved R2/R4 T1 control and `eval_codec.py` evaluates it. DiT construction, training and generated validation run through `scripts/run_state_detail_dit_pilot.py`, `trainer/dit_trainer.py`, `scripts/run_dit_architecture_sequential_v1.py` and the Round 2/4 runners. `scripts/run_dit_architecture_round4.py` owns observed-prefix rollout. They call `module/latent_rectified_flow.py` and `evaluation/dit_evaluation.py`; the current RMSF reader is `evaluation/motion_metrics.py`. `scripts/preprocess_trajectory_clips.py` is the ordered-clip preprocessing entry. `config/dit_architecture_sequential_v1.yaml` selects R4, conditional `repeat_last_coordinate_encode`, factorized backend, H4/H8, 16 frames and frozen codec/statistics. `config/codec.yaml` defaults to legacy mode, so it is not by itself a working R4 control; mode is dispatched in `trainer/codec_trainer.py` and validated against the checkpoint contract.

The following definitions were checked in the selected HEAD. Python path renames and state-dict key migration are separate operations.

| Old definition | Destination |
|---|---|
| `trainer/codec_trainer.py::PVBCodecModel` | `molvid/codec/model.py::TrajectoryCodec` |
| `module/multiframe_codec.py::PVBFrameEncoder`, `FrameEncoderOutput` | `molvid/spatial/encoder.py::FrameEncoder`, `FrameEncoderOutput` |
| `module/multiframe_codec.py::FrameGraphBatch` (`PVBFrameGraph` is an alias), `FrameNodeBatch` | `molvid/geometry/types.py`; remove alias after callers migrate |
| `module/torchmd_et.py::TorchMD_VQ_ET` | `molvid/spatial/torchmd.py::TorchMDEncoder` |
| `module/state_detail_codec_v2.py::StateDetailCodecV2`, `MatchedPoolingCodecV2` | `molvid/codec/state_detail.py::StateDetailCodec`, `MatchedPoolingCodec` |
| `module/state_detail_codec_v2.py::StateDetailLatent` (`StateDetailLatentV2` is an alias), `MatchedPoolingLatent`, `StateDetailDecoderOutput` | `molvid/codec/types.py::StateDetailLatent`, `MatchedPoolingLatent`, `CodecOutput`; remove alias after callers migrate |
| `module/state_detail_codec_v2.py::HaarLift` and `haar_lift`/`haar_inverse` | `molvid/codec/haar.py::HaarCoefficients` and functions |
| `module/state_detail_codec_v2.py::CenteredCoordinateVectorStem`, `_EquivariantCoordinateHead` | `molvid/codec/heads.py::CoordinateVectorStem`, `EquivariantCoordinateHead` |
| `module/state_detail_latent_adapter.py::LatentFieldSet`, `DiTLatentBatch` | `molvid/latent/types.py::LatentFields`, `LatentBatch` |
| `scripts/run_state_detail_dit_pilot.py::FrozenCodec`, `PilotData`, `ValidationPlan` | `molvid/checkpoints.py::CodecArtifact`; `molvid/data/manifest.py::DatasetSplits`, `ValidationPlan` |
| `scripts/run_dit_architecture_round4.py::PreparedRound4Batch` | `molvid/training/batches.py::PreparedDiTBatch` |
| `data/mmap_dataset.py::MMAPDataset` | storage reader in `molvid/data/store.py::BlockStore`; static record conversion remains in data boundary |

Other retained ownership resolved from definitions and callers: `data/clip_dataset.py` supplies `batch.py` and clip store classes in `store.py`; `data/clip_batching.py` plus `scripts/run_state_detail_codec_v2_t1.py::TrajectoryCappedBatchSampler` supply `sampling.py`; `data/trajectory_clips.py` and its preprocess CLI supply `io.py`, `preprocess.py` and split-manifest behavior; `module/multiframe_codec.py` supplies frame packing, geometry types and encoder; `module/neighbor_graph.py`, `topology_cache.py` and `bond_sources.py` supply geometry neighbors/topology; `module/state_detail_latent_adapter.py` supplies adapter, statistics and observation condition; `module/molecular_dit.py` plus `dit_backend_v2.py` supply model, blocks and reference/optimized backend; `module/latent_flow_source.py` and `latent_rectified_flow.py` supply source/objective/sampling; `module/dit_history_corruption.py` supplies corruption views; `module/dit_geometry_supervision.py` supplies future-bond auxiliary; `trainer/codec_losses.py` supplies reconstruction losses; `trainer/codec_trainer.py` and `trainer/dit_trainer.py` supply separate trainers; `evaluation/{codec_evaluation,dit_evaluation,dit_diagnostics,motion_metrics}.py` and the current runners supply evaluation/generation. YAML, runner stage dispatch and checkpoint contracts are active dependencies even where there is no static import. These paths must be rechecked immediately before P5 retirement.

### Available historical numerical reference

- Approved R4 codec: `PVB/outputs/state_detail_codec_v2/t1/full_20260904_seed20260903/ratio4_state_detail/codec_best.pt`, SHA256 `ba10c44189cca837430abbd64afce2109a0daf0bda4f05971e0441abb2a5e6df`. Its existing `result.json` reports seed `20260903`, 45,844 steps, full future aligned RMSD `0.07189485111558085` Å and bond RMSE `0.013008868230377535` Å; its detail-zero control reports `0.9256625400942147` Å and `0.12391022327708508` Å. The recorded resume check loaded step 45,844 and continued to 45,845. These are historical run results, not a new migration comparison.
- Frozen R4 statistics: `molvid-dit-state-detail-pilot-v1/outputs/dit_state_detail_pilot_v1/full_20260908_S4500/ratio4_state_detail/shared/statistics.pt`, SHA256 `4d07bf53f317a6f2937a027695d2d70993674903d0fcefc1fa49e0482a529583`. The frozen manifest directory exists at `PVB/outputs/state_detail_codec_v2/t1/manifest_20260904_token80000`; only its metadata was inspected. Protocol records 48 train, 8 validation, 8 sealed test systems.
- Existing DiT baseline checkpoint in the source worktree: `outputs/dit_architecture_sequential_v1/20260914_r4_architecture_sequential_v1/baseline/checkpoint_final.pt`, SHA256 `808eb51e5c597629ebbd7270697af91affbc82a812cadd165eae984cba900240`. Its summary reports `PASS`, 4,500 successful updates, model hash `af0c71a3a0d2c460b899b615da86b054bc56eed3b5a7160e9b4b9005d33df643`, adapter hash `fc81cb9aba877c58571c74189fd0a6054e717671f9b2158216616d597f706bea`, and statistics hash `863c1894bac27c6fa121eaaa635961e44c37cc95c9f55b3fb371b4fb0bab778d`. Its recorded quick generated validation (system-equal aggregation) gives H4/H8 aligned RMSD `3.721147636503091`/`3.750142274852074` Å and bond RMSE `3.541726608890788`/`3.530757985007465` Å. These scalar outputs are useful regression context but cannot establish same-input tensor or gradient parity.

### Checks and gate

- Passed read-only: selected HEAD/status, direct source definitions, config/runner call paths, artifact existence and SHA256 for codec, statistics and DiT baseline checkpoint. GPU devices are visible to `nvidia-smi` (A100); no GPU work was launched.
- Passed fresh in `enter-container` → `conda activate torch-ito` with PyTorch `2.5.1+cu121` on CUDA GPU 5: 60 targeted old-path tests passed, with one existing `torch.load` future warning and no skip. The command set `PYTHONDONTWRITEBYTECODE=1 CUDA_VISIBLE_DEVICES=5 CUBLAS_WORKSPACE_CONFIG=:4096:8 PYTHONHASHSEED=0 DIT_RUN_CUDA_BACKEND_V2=1 DIT_RUN_CUDA_CORRECTNESS=1 DIT_RUN_ARCHITECTURE_SEQUENTIAL_V1=1` and ran `python -m pytest -q -p no:cacheprovider` on `test_clip_data.py`, `test_multiframe_codec.py`, `test_multiframe_codec_equivariance.py`, `test_codec_training.py`, `test_state_detail_latent_adapter.py`, `test_dit_trainer.py`, `test_dit_backend_v2.py`, `test_dit_cuda_correctness.py`, `test_dit_architecture_round4_cuda.py` and `test_dit_architecture_sequential_cuda.py`.
- Fixed tiny R4 codec CUDA oracle: seed 713; 16 frames × 4 atoms, 100 ps spacing, chain bonds, tetrahedron coordinates plus `0.03*sin(0.4*t+a)`; width 8, one TorchMD layer, CUDA radius neighbors. Old frame scalar/vector sums and norms are `(-1.7285346984863281e-6,22.627260208129883)` and `(-16.39719009399414,21.704484939575195)`; state/detail scalar sums and norms are `(48.443519592285156,12.577531814575195)` and `(0.001795983873307705,0.034256286919116974)`. Decoded coordinate sum/norm `(51.094364166259766,3.7128775119781494)`; coordinate MSE `0.1662708967924118`. Gradient norms: coordinate stem projection `0.03537152335047722`, coordinate head output `0.26573774218559265`, TorchMD atom embedding `0.009136345237493515`. P2 must compare full tensors and gradients with the same weights, beyond these checksums.
- Fixed tiny R4 DiT CUDA oracle: `make_batch(4,width=4,seed=11)`, H8 observed, model seed 902, flow generator seed 77, factorized backend, depth 1, width 8/4; AdamW LR `0.0002`, decay `0.01`. First tau `[0.20616373419761658,0.6913903951644897]`; velocity field norms `(state_h,detail_h,state_v,detail_v)` are `(11.47791862487793,13.894954681396484,14.727928161621094,10.575072288513184)`. Four-field loss `3.2482223510742188`, gradient norm `2.7454140186309814`, maximum one-step parameter change `0.00020396709442138672`. Next tau `[0.4522126019001007,0.5367502570152283]`, next loss `2.835012197494507`. In-memory save/load of model, optimizer and generator state into a new model gave identical next loss/gradient and zero maximum parameter difference. The serialized generator state must be loaded on CPU before `Generator.set_state`; the first one-off harness attempt mapped it to CUDA and failed, then this was corrected.
- Actual artifact wrappers: codec `pvb.codec.checkpoint.v2` at step 39,721 has 57 model keys, one 57-parameter optimizer group and moment state for 15 trainable parameters; sampler state and optimizer contract are present, explicit RNG state is absent. DiT `pvb.dit.state_detail.checkpoint.v2` at step 4,500 has 166 model keys, one 166-parameter optimizer group with 166 moment states, Python/NumPy/Torch/CUDA RNG, scaler, scheduler and sequential cursor/training-generator state. P2/P4 must validate these distinct layouts instead of reconstructing an empty optimizer.
- `not_run`: new-path tensor/gradient/weight/optimizer parity (P2–P4) and final package/import/rollout acceptance (P5), because those modules have not been migrated. Both worktrees are mounted in the container. Keep original checkpoint/data bytes unchanged; no tolerance change, deletion or fallback is authorized by this record.

## P1a — data contract, storage and sampling (2026-09-18)

Status: local sub-checkpoint only; P1 geometry, preprocessing, manifest, chemistry, runtime/config and full P1 acceptance are still pending. No old file or runner has been switched or deleted.

- Added the root `molvid/` package, explicit setuptools discovery limited to `molvid` and `molvid.*`, and extracted `data/batch.py`, `data/store.py`, `data/sampling.py` from the active implementations. The clip-store schema string `pvb.clip.v1` and the trajectory sampler state schema remain unchanged intentionally so existing bytes and resume state remain readable; these are serialized contracts, not public class names. Static records still read the existing gzip/mmap bytes as T=1 without replicating coordinates. Removed optional sampler aliases from the new package.
- New-path assertions migrated to `tests/test_data.py` and `tests/test_data_sampling.py` without dropping the old shape, timestamp, mask, atom-order, bond-offset, static-store, T*N, DDP and no-tail-drop checks. Added a two-trajectory capped-sampler selection/resume check.
- Passed in `enter-container` / `torch-ito`: `PYTHONDONTWRITEBYTECODE=1 python -m pytest -q -p no:cacheprovider tests/test_data.py tests/test_data_sampling.py` → 18 passed. `setuptools.find_packages(include=["molvid", "molvid.*"])` returned only `['molvid', 'molvid.data']`. Importing these three new data modules in a fresh interpreter loaded no old `data.*` or `module.*` package.
- One first-pass test fixture accidentally wrote literal backslash-t separators in its index; it was corrected and all tests reran. This was a fixture error, not a reader behavior change.
- `not_run`: full P1 data/geometry acceptance and real-store byte parity. Next: extract trajectory I/O/preprocessing and chemistry; then geometry/shared operators; run old/new fixed-input comparisons and complete P1 gate before P2.

## P1b — geometry, chemistry and shared vector operators (2026-09-18)

Status: second local P1 sub-checkpoint; P1 remains open and no production entrypoint or legacy file has been switched/deleted.

- Extracted frame packing/graph construction, CUDA radius neighbors, bounded topology/distance-bond caches, static topology metadata, coordinate alignment/centering and the two distinct equivariant operators into the flat package. The duplicate topology-id helper was consolidated into `molvid.geometry.topology`; clip collation now imports that one implementation. Serialized `pvb.*` schema identifiers remain intact for existing stores and artifacts.
- Added new-path CUDA graph tests for 2 samples × 2 frames: 36 isolated edges, 8 replicated covalent edges, nonzero finite coordinate gradients. Also tested static metadata excluding coordinates/radius edges, train-only frame-zero distance references remaining unchanged when future coordinates are perturbed, T=1/static chemistry type IDs and RDKit bond order, centering/restoration and rotation behavior, and deterministic vector-linear initialization/RNG consumption.
- Passed in `enter-container` / `torch-ito`: `PYTHONDONTWRITEBYTECODE=1 CUDA_VISIBLE_DEVICES=5 python -m pytest -q -p no:cacheprovider tests/test_data.py tests/test_data_sampling.py tests/test_geometry.py` → 24 passed. Fresh imports of all new geometry/chemistry modules loaded no old `data`, `module`, `utils`, `trainer` or `evaluation` package.
- `not_run`: actual trajectory preprocessing and real-store parity, full P1 gate, downstream frame-feature/codec parity. Next: move trajectory I/O/preprocess, split manifest, runtime/config; test real clip metadata and complete P1 before P2.

## P1 — complete (2026-09-18)

Status: all 15 P1 runtime files are present in the root `molvid/` layout, with no `src/` or `molvid/models/`. No legacy runner was switched and no old file deleted.

- Migrated ATLAS XTC and MISATO trajectory reading, static-store conversion, ordered clip preprocessing, frozen split loading/planning, explicit YAML path resolution and shared CUDA/RNG/result-writing helpers. The frozen legacy manifest schema is read only to migrate existing artifacts; new manifests use `molvid.clip.manifest.v1`. No test payload is opened by `load_datasets()`.
- Passed in `enter-container` / `torch-ito`: `PYTHONDONTWRITEBYTECODE=1 CUDA_VISIBLE_DEVICES=5 python -m pytest -q -p no:cacheprovider tests/test_data.py tests/test_data_sampling.py tests/test_data_preprocess.py tests/test_manifest.py tests/test_geometry.py tests/test_runtime_config.py` → 31 passed. The tests cover physical ps/Å conversion, atom order, masks, static T=1, split overlap/sealing, deterministic sampler resume, CUDA graph isolation and coordinate gradients, vector initialization/RNG order. Importing all 15 new modules in a fresh interpreter loaded no old root package; package discovery returned only `molvid`, `molvid.data`, `molvid.geometry`.
- Independent old/new CUDA processes on the same two-sample, two-frame fixture produced byte-identical SHA256 for graph ids, edge indices/weights/vectors, bond types and replicated bond indices (36 edges, 8 covalent edges). The edge-index hash is `8e0bdd8fcec85e82f2667a356a00a8654ea5c4acb4d062ded235fd1950663334`.
- Independent old/new reads of the frozen T1 manifest matched 8,928 train and 1,488 validation clips, data contract hash `9daaf83fe5ee862634f7d1d3530e730adb4a8529ed37a2bc2fd330304ebfe184`, both index hashes, and the 72-sample/26-batch validation plan hash `a3bb28953e1933b059f86753f2c09909b2e49508a79840e705032265b743c299`. Old/new decoding of the first train and validation records matched metadata and `x`/`time_ps`/`bond_index` bytes. New raw ATLAS preprocessing of train sample `atlas_1by2_A_R1_w000000` matched the frozen store byte-for-byte on 13 coordinate/time/type/order/mask/bond fields. The sealed test payload remained unopened.
- `not_run`: full raw-dataset re-materialization (unnecessary for this targeted P1 gate), P2 frame-feature/codec/optimizer parity and later training/generation gates. Next: P2 spatial/codec extraction and same-weight tensor/gradient/checkpoint comparisons before any dependent cutover.


## P2 — codec, spatial backbone and strict checkpoint migration (2026-09-18)

Status: all nine P2 runtime files are present under the root flat package and the P2 gate passed. P3 may start. No legacy file or production runner was switched or deleted; the original artifacts were read only. The P2a spatial sub-checkpoint is recorded above.

- Extracted fixed Haar R1/R2/R4, structured latents, matched-pooling control, centered-coordinate stem and coordinate head. The temporal modules no longer own the coordinate head; the model owns it after the temporal banks, preserving effective initialization and RNG consumption. The latent topology now uses the single `molvid.geometry.types.StaticTopologyMetadata` class, so coordinate-independent metadata remains in contracts.
- Exact same-seed old/new tiny CUDA comparisons with deterministic algorithms: identical parameter tensor order/values, CPU RNG state, frame graphs/features, Haar coefficients/inverse, R1/R2/R4/matched-pooling decoded outputs, R4 latent banks, coordinate MSE, input and parameter gradients. Frozen frame-encoder parameters had `grad is None`; coordinate/input gradients remained active.
- Strict anchored checkpoint mapping was validated on the approved R4 file (SHA256 `ba10c44189cca837430abbd64afce2109a0daf0bda4f05971e0441abb2a5e6df`): 57/57 weights and optimizer parameters, all 15 AdamW moment states, step 39721, sampler cursor and 42 frozen/15 trainable parameters. New/old same-weight CUDA latent/coordinate/loss/gradient and one AdamW step/moments matched at zero tolerance. Missing keys, duplicate target mapping, shape/dtype mismatches and wrong source hash fail explicitly; there is no `strict=False` recovery.
- Other retained real control checkpoints also migrated with their fixed source hashes: R1 `d697933d467e4e176ebcf024ed69a59c518460eb486b3f32ad6ea10e84931a68` (52 weights/10 moments), R2 `b15cb92c34aec0e0f0c44e796def518d7ad89cda3dbc7f2fc3de83d55b4c64e9` (57/15), matched R4 `6c9f84420ae81673904fc8a1e838d1298e8d598765ece53913048fd70bb6ba8a` (56/14).
- New-format atomic checkpoint save/load records model, optimizer parameter names/order, scheduler, scaler, RNG, sampler state, cursor and contracts. A short CUDA save/resume test matched Python/NumPy/Torch/CUDA next draws, next loss, model, optimizer moments and scheduler exactly. Fresh new-module imports loaded no legacy root packages; package discovery was limited to `molvid` and four current subpackages.
- In `enter-container` / `torch-ito`, `tests/test_codec.py tests/test_migration.py` passed 15 tests; the combined P1+P2 regression passed 46 tests on CUDA GPU 5. Exact tensor checks used `CUBLAS_WORKSPACE_CONFIG=:4096:8` and deterministic algorithms, not widened tolerances.
- `not_run`: bitwise continuation of the historical codec checkpoint, because that old format has no explicit RNG state; long training; P3 observation/future-isolation and DiT parity; P4 end-to-end trainer resume; P5 final entrypoint/import/retirement checks. Keep the old codec trainer/runner and files in place until P4/P5 acceptance. Next: P3 latent/condition, DiT and flow extraction, with frozen artifact and future-isolation checks before any cutover.


## P3a — latent contracts and observed-history condition sub-checkpoint (2026-09-18)

Status: latent adapter, statistics and conditioning extraction passed its local checks; P3 DiT/flow and full inference-isolation gates remain pending. No old file or runner was switched/deleted.

- Added `molvid/latent/{types,adapter,statistics,conditioning}.py` with direct imports from the flat codec/geometry/equivariant packages. Renamed the two public data contracts to `LatentFields` and `LatentBatch`, without aliases; retained serialized schema strings and exact hash math so existing statistics artifacts stay verifiable.
- Same-seed old/new R2/R4 adapter tests matched state keys/values, subsequent RNG, packed latent contracts, scalar/vector projections and parameter gradients at zero tolerance. Train-split statistics fit, normalization, inverse and artifact hash matched. Generated latents exclude raw detail and target coordinates.
- H0/H4/H8 R2/R4 observation masks and origins matched the old implementation at zero tolerance. Mutating only true future coordinates left the new `ObservationCondition` unchanged. The online H8 corruption test matched sigma selection, applied noise and clean/condition origin gauge; `target_view` and clean target latent fields were not overwritten. The formerly hard-coded 50/25/25 probabilities are now a validated input (same default and 0/0.02/0.05 Å sigma choices), ready for P4 configuration.
- In `enter-container` / `torch-ito`, `tests/test_latent.py tests/test_conditioning.py` passed 10 tests. Fresh imports loaded no legacy root package.
- `not_run`: full DiT/flow inference condition isolation, reference/optimized backend parity and generation rollout. Next: extract the DiT blocks/backends and flow with fixed-weight/tau/noise parity, then complete the P3 gate before P4.

## P3b — DiT blocks, reference and factorized backends (2026-09-18)

Status: local P3 sub-checkpoint; flow and end-to-end future-isolation are still pending. No old runner or file was switched/deleted.

- Extracted the parameter blocks, reference execution and factorized CUDA layout/backend into `molvid/dit/{model,blocks,backend}.py`. The model retains the same adapter object, parameter names/order, semantic contract and effective initialization/RNG order; `build_dit()` applies the current runner's explicit model settings.
- Same-seed old/new R2/R4 tests on CUDA matched all 55 model state keys and values, subsequent CPU RNG, fixed-tau velocity fields, loss, every parameter gradient and one AdamW update at zero tolerance for both execution backends. New reference/factorized forward and gradient checks retained the existing pre-refactor tolerances (2e-5 and 2e-4); they were not widened.
- The old reference backend triggers a PyTorch internal indexed-assignment assertion with deterministic algorithms enabled. That old/new reference comparison therefore uses its original non-deterministic setting; the factorized comparison runs with deterministic algorithms. Both still matched exactly on the tested fixture. A fresh import of the new DiT modules loaded no legacy root package.
- In `enter-container` / `torch-ito`, `tests/test_dit.py` passed 6 tests; combined P1–P3b regression passed 62 tests. `not_run`: historical DiT checkpoint optimizer-state migration and resume (P4), flow parity and full inference future-isolation (P3c), generation rollout (P4/P5). Next: extract the flow mathematics/source/sampling and complete P3 gate.

## P3c / P3 — flow, source and inference isolation complete (2026-09-18)

Status: all ten P3 runtime files exist in the root flat package; P3 acceptance passed. No legacy runner, trainer or source file was switched/deleted.

- Extracted four-field interpolation, velocity, masked loss, endpoint formula, Gaussian/conditional source, observed-only center and clamped 8/16-step Euler sampling into `molvid/flow/{objective,source,sampling}.py`. The old source-contract class was removed; the `source_contract()` output and serialized `pvb.*` schema string remain exactly compatible with existing artifacts. Production flow imports have no dependency on old root packages and form an acyclic source → sampling → objective dependency.
- Fixed-generator CUDA R2/R4 old/new comparisons matched tau, next generator state, all noise/source/interpolated/target fields, flow loss and valid counts, gradients and endpoint at zero tolerance. A DiT-driven H8 conditional 8-step Euler rollout matched the old result and metadata exactly; observed fields remained clamped. Replacing only true future latent values left the center and rollout identical.
- Real 16-frame old/new codec encodes of a repeated-last-observed-coordinate center matched exactly under deterministic CUDA. Mutating only future raw coordinates left the new template and encoded center unchanged. Both Gaussian/conditional source-contract dictionaries matched the old implementation exactly; invalid mode failed explicitly. These checks supplement P3a's H0/H4/H8 observation and clean-target isolation.
- In `enter-container` / `torch-ito`, `tests/test_flow.py` passed 6 tests; the P1–P3 suite will be rerun before this commit. `not_run`: historical DiT checkpoint/optimizer-state migration and full trainer resume, frozen codec decode gradient behavior, longer generation/evaluation/CLI and P5 retirement. Next: P4a loss and codec trainer, then P4b DiT training; keep old production entrypoints active until their gates pass.

## P4a1 — reconstruction losses and time-bucket normalization (2026-09-18)

Status: loss-only sub-checkpoint; codec trainer and full P4a training/resume gate remain pending. No old trainer/entrypoint was switched or deleted.

- Extracted coordinate, local/contact, bond, physical-time velocity/acceleration losses, guarded train-bucket normalization and `TimeBucketSpec` into `molvid/losses/reconstruction.py`, importing only the new clip contract. The old public PVB name is absent; serialized clip/time schemas remain readable.
- CUDA old/new static and nonuniform-trajectory comparisons matched all loss components and total gradients at zero tolerance with fixed weights/normalization. Masked atoms, static temporal zero and small-count normalization fallback matched; time-bucket contract matched. In `enter-container` / `torch-ito`, `tests/test_losses.py` passed 4 tests, and fresh loss imports loaded no legacy root package.
- `not_run`: codec trainer one-step/new-format resume and frozen-state behavior; future-bond auxiliary and DiT training; generation/evaluation/CLI. Next: extract `CodecTrainConfig`/`CodecTrainer` on the new package, use the strict shared checkpoint writer/loader, and compare one step and next resumed step before proceeding to P4b.

## P4a — codec trainer and strict new-format continuation (2026-09-18)

Status: P4a loss/codec-training gate passed; the old codec trainer/runner remains active and untouched. A fresh historical old-format codec continuation is still `not_run` because its checkpoint has no explicit RNG state.

- Extracted `CodecTrainConfig`, `CodecTrainer`, `train_codec()` and CPU-preparation-before-device-transfer into `molvid/training/{codec,batches}.py`. Configuration keeps the existing serialized YAML schema, while new training logs use `molvid.codec.train.v1`. New checkpoints use the shared atomic `molvid.training.checkpoint.v1` writer with Python/NumPy/Torch/CUDA RNG, model/optimizer parameter names/order, normalizations and cursor; no old-format fallback was added to the new trainer.
- Strengthened the shared checkpoint loader to reject duplicate/unknown optimizer parameter IDs, mismatched group hyperparameters, malformed AdamW steps/moments, frozen-parameter moments and shape/dtype corruption before state mutation. The trainer validates config, warmup LR, normalization and sampler/cursor first, and snapshots model/optimizer/sampler/RNG for rollback. The codec contract now records frozen/source identity and registered distance-reference contract where applicable without changing parameter initialization order.
- Deterministic CUDA old/new tiny frozen-frame-encoder trainer steps matched metrics, weights, all AdamW groups/moments and missing frozen gradients at zero tolerance. New-format save/load restored next Python/NumPy/Torch/CUDA draws, next loss/update, model and optimizer exactly. A corrupted moment was rejected with model, optimizer, cursor and RNG unchanged. A two-batch mid-epoch run resumed the correct next batch and optimizer state. `tests/test_training.py` passed 4 tests; combined P1–P4a suite passed 76 tests; fresh trainer imports loaded no old root package.
- `not_run`: historical codec bitwise continuation (missing old RNG), registered distance-only reference resume, P4b DiT checkpoint/gradient gate, P4c generation/evaluation and P4d CLI. Next: move DiT batch preparation and trainer with full 166-parameter/moment checkpoint migration, then future-bond supervision.

## P4b1 — DiT batch boundary and future-bond gradient path (2026-09-18)

Status: local P4b sub-checkpoint; DiT trainer and historical optimizer migration remain pending. No old runner/source was switched or deleted.

- Extracted the retained per-sample future covalent-bond supervision into `molvid/losses/geometry.py`; endpoint estimation is reused from `molvid.flow.objective`. Its eligible/future/mask accounting and physical-unit loss are unchanged. A deterministic CUDA old/new comparison matched raw auxiliary loss, diagnostics and every DiT gradient at zero tolerance; the frozen decoder received no parameter gradients or state changes while gradients reached DiT.
- Added `PreparedDiTBatch`, `encode_batch()` and `prepare_dit_batch()` to the existing shared training batch boundary. Static topology is prepared on CPU before transfer; clean target, online noisy observed condition, and observed-only conditional center remain separate. On a real 16-frame codec fixture, changing only true future coordinates left the conditional source and all observed latent fields unchanged, while the target view remained clean.
- In `enter-container` / `torch-ito`, the updated `tests/test_losses.py` and `tests/test_training.py` passed 10 tests. `not_run`: DiT trainer one-step and 166-moment historical migration/resume; generation/evaluation/CLI. Next: extract the DiT trainer with strict checkpoint mapping and compare old/new fixed-generator updates before any cutover.

## P4b2 / P4b — DiT trainer and historical optimizer gate (2026-09-18)

Status: local P4b checkpoint; old runner and source files remain active. The new trainer owns only flat-package dependencies, keeps codec/frame encoder frozen, and writes a strict new-format checkpoint with optimizer, scaler, statistics, generator, global RNG and cursor.

- Deterministic CUDA old/new fixed-generator DiT one-step metrics, weights and optimizer state matched exactly. New-format save/load reproduced the next RNG draws and optimizer update. The approved historical baseline (SHA-256 `808eb51e5c597629ebbd7270697af91affbc82a812cadd165eae984cba900240`) transferred 166 ordered weights and 166/166 AdamW moment slots after schema, contract, stats, provenance and cursor checks; fixed-batch source/tau/velocity/loss/gradients and one update matched the old path at zero tolerance. Wrong digest is rejected before deserialization.
- `enter-container` / `torch-ito`: combined P1–P4b regression 82 passed. Fresh flat trainer imports do not load old root packages; `git diff --check` passed. `not_run`: bitwise historical data/sampler-dependent DiT continuation, because the full original frozen codec/frame-encoder state and exact next sample stream have not been reconstructed in the new path; historical codec continuation still lacks RNG. Do not switch old runners or retire those dependencies on this evidence. Next: P4c short observed-only generation/rollout and metric extraction, then P4d CLI/config.

## P4c1 — observed-only sample and short rollout (2026-09-18)

Status: generation sub-checkpoint; metric/evaluation extraction remains open. No old runner or source file was switched/deleted.

- Added sample_clip and rollout at the flat package boundary. The only coordinate input is an H4/H8 prefix; all 16 template coordinates, including any true future, are replaced by an observed-prefix repeat before encoding. The latent solver clamps observed fields, decoded coordinates clamp the physical prefix, and later rollout segments receive only their own generated eight-frame prefix. Codec must be in eval mode and share the DiT device; DiT/adapter identity and statistics device are checked.
- In enter-container / torch-ito with deterministic CUDA, tests/test_generation.py passed 3 tests and combined P1–P4c1 regression passed 85. H4/H8 generation was bitwise unchanged when all template future x/bpos values were mutated. A two-segment rollout matched separately seeded sample_clip calls exactly; prefixes and frozen-module gradients were checked. Existing P3 old/new Euler equivalence remains the numeric solver reference. not_run: reference-checkpoint full generation, quantitative metrics, complete historical rollout and P4d CLI. Next: extract geometry/motion/latent metrics, evaluation runner and report without importing legacy evaluation packages.

## P4c2 — geometry and motion metrics (2026-09-18)

Status: metric sub-checkpoint; latent/evaluation runner/report and CLI remain pending. No legacy evaluation path was switched/deleted.

- Moved the existing masked coordinate, Kabsch-aligned RMSD, dRMSD, bond/contact/clash/torsion and temporal reductions into flat evaluation modules, preserving the original math and cutoff/exclusion definitions. Exposed coordinate_errors, bond_errors, contact_scores, clash_statistics and motion wrappers. Kept the corrected RMSF reader's distinction between valid zero, missing/non-finite, and contradictory representations, including historical result interpretation.
- In enter-container / torch-ito, tests/test_metrics.py and tests/test_generation.py passed 15 tests; combined P1–P4c2 regression passed 97. The full metric record matched old evaluation exactly on CPU and real CUDA for H0/H4/codec intervals; no tolerance was relaxed. not_run: reference-checkpoint quantitative generation, latent/oracle gap, evaluation runner/report and P4d CLI. Next: move latent diagnostics and route evaluation/reporting through the new package; keep the old runner active until that passes.

## P4c3 / P4c — latent, evaluation runner and report (2026-09-18)

Status: P4c code gate passed on tiny deterministic CUDA clips; old evaluation runners remain active. The original full-weight/data-scoped generation comparison is not yet established and is not a cutover claim.

- Added future-only four-field latent errors, explicit codec-oracle/generated/gap records, and an evaluation runner with separate codec, one-clip generation and generated-prefix rollout paths. RolloutTrack keeps reference coordinates on the scoring side; the generator receives only the first observed prefix. Reports aggregate draw -> clip -> system and split physical-time buckets before plotting/writing.
- In enter-container / torch-ito, tests/test_evaluation.py passed 5 tests and combined P1–P4c regression passed 102. Full forecast metric records matched the old evaluator exactly for H0/H4/H8 and codec intervals; aggregate-generated rows matched the old reduction. On deterministic CUDA, changing future reference coordinates changed scores but left the generated two-segment trajectory bitwise identical. The initial repeated-run assertion without deterministic CUDA differed by one float at 5.96e-8; enabling the same deterministic CUDA policy used by earlier numerical gates resolved it without tolerance changes.
- not_run: same-input full historical codec+DiT checkpoint generation against the old runner, real validation-system rollout and metrics, historical data-cursor continuation. These results are required before old generation/evaluation entrypoints or source files can be retired. Next: P4d CLI/config/tools on the new package, followed by P5 full-chain/import audit and resource-dependent retirement decision.

## P4d1 — preprocess CLI and source-manifest helper (2026-09-18)

Status: local P4d sub-checkpoint; training/sample/evaluate CLI and the production entrypoint switch remain pending.

- Moved the active ATLAS/MISATO CLI orchestration to molvid.cli.preprocess, using already-migrated raw readers, clip writer and deterministic split logic. The source-inventory and physical-time split-manifest helpers were moved into molvid.data.manifest before the old script can be retired. No raw data was preprocessed or overwritten.
- In enter-container / torch-ito, the module CLI --help worked and tests/test_cli.py, tests/test_manifest.py, tests/test_data_preprocess.py passed 6 tests. On the same source-id fixture, old/new split selection, inventory and manifest bytes matched exactly. not_run: full ATLAS/MISATO preprocessing, which requires operator-selected raw roots and output destination. Next: functional flat-package codec/DiT training and sampling/evaluation CLI with explicit config rejection of retired modes.

## P4d2 — codec training CLI and current config (2026-09-18)

Status: local P4d sub-checkpoint; DiT train/sample/evaluate entrypoints remain pending. The historical root train_codec.py is untouched and active.

- Added molvid.cli.train_codec and a separate current ratio4_state_detail YAML. It routes train/valid stores, task-aware batching, train-only distance-reference preparation, normalization, one-step dry run, new-format checkpoint save and SHA-checked resume through migrated modules. Retired legacy/refiner constructor settings are rejected rather than aliased. New codec config serialization uses molvid.codec.config.v1; the old/new config parity test now explicitly maps only that intentional schema rename and still compares every scientific setting exactly.
- In enter-container / torch-ito, tests/test_training.py and tests/test_cli.py passed 10 tests. A temporary four-frame store completed dry run, one CUDA optimizer update, checkpoint save and a resumed second update with the expected new schema/step. No long training or user dataset write was performed. not_run: full training on real data and historical codec bitwise continuation (old checkpoint lacks RNG). Next: DiT CLI with observed-history probabilities wired to batch preparation, then sample/evaluate CLI.

## P4d3 — DiT training CLI and real-codec short resume (2026-09-18)

Status: local P4d sub-checkpoint; sample/evaluate CLI and P5 are pending. Historical DiT runner has not been switched or deleted.

- Added molvid.cli.train_dit with explicit approved codec/statistics hashes, frozen train/valid manifest, R2/R4 model, H4/H8 order, configurable 50/25/25 history corruption, separate RF/corruption generators, optional future-bond auxiliary, and strict new-format cursor/sampler/generator checkpoint resume. Sampler schedule, deterministic policy and geometry settings are part of the new checkpoint contract. Historical full-weight DiT continuation is not claimed.
- In enter-container / torch-ito, tests/test_cli.py and tests/test_training.py passed 11 tests. A temporary frozen manifest and the approved real R4 codec checkpoint ran a no-update preparation, one CUDA optimizer step, resumed step 2, and uninterrupted two-step reference. All DiT weights and AdamW slots matched exactly at zero tolerance, with the test split payload unopened. An initial non-deterministic CUDA run differed at ~6e-8; enabling the established deterministic CUDA policy made the unchanged zero-tolerance comparison pass. No long training was run.
- not_run: 4,500-step historical DiT data/sampler continuation, original full-weight DiT generation on real validation systems, and CLI source-arm/geometry experiment protocols beyond the short path. Next: add sample/evaluate CLI with explicit observed-only input and artifact validation, then P5 package/import and retirement audit.

## P4d4 — observed-only sample/evaluate CLI (2026-09-18)

Status: P4d implementation gate passed in the isolated branch; historical runners remain active and untouched pending P5 evidence.

- Added SHA-verified new-format DiT inference loading with full optimizer/statistics/frozen-codec contract checks. Construction restores caller CPU/CUDA RNG state. Added sample/evaluate CLI using the frozen validation manifest, explicit prefix-only NPZ for generation, source-arm settings from the training checkpoint, and regular-grid checks before rollout time export. The generation template erases held-out future coordinates; evaluation truth is scorer-only.
- In enter-container / torch-ito on deterministic CUDA, the real approved codec plus short-trained DiT produced finite H8 sample and rollout artifacts with exact observed prefixes; prefix NPZ with a future field was rejected. Oracle/generated metrics and time-bucket report were written, and the sealed test split remained unopened. The resumed/uninterrupted two-step DiT weights and all optimizer slots still matched at zero tolerance. Full retained P1–P4d suite: 106 passed in 137.39 s; git diff --check passed.
- not_run: full-weight historical DiT generation against the old runner, real validation-system rollout, and historical data/sampler continuation. These are required before switching/deleting the old generation/evaluation paths. Next: P5 package-outside import isolation, short CUDA profile, documentation and a retirement reachability audit; do not infer numerical sign-off from the tiny CLI run.

## P4d5 — versioned sample/evaluation configs (2026-09-18)

Status: local CLI follow-up checkpoint; P5 retirement audit remains.

- Added strict versioned YAML-to-CLI argument parsing for sample and evaluation, with unknown-field rejection, argparse type/choice checks and paths resolved relative to the YAML file. Moved the newly introduced training templates into configs/codec_train.yaml and configs/dit_train.yaml, and added configs/sample.yaml and configs/evaluate.yaml. The historical config/ files were not changed. Direct explicit CLI flags still work.
- In enter-container / torch-ito, tests/test_runtime_config.py tests/test_cli.py tests/test_imports.py passed 10 tests, including relative-path YAML, SHA-verified short CUDA generation and evaluation, and outside-repository import isolation. not_run: full historical checkpoint generation and real-system rollout; new YAML templates intentionally contain operator-supplied placeholder paths. Next: P5 full retained suite, short CUDA profile and legacy dependency audit; no old runner switch yet.

## P5 checkpoint — import audit and real-CUDA parity blocker (2026-09-18)

Status: P5 numerical sign-off, old-entrypoint cutover and retirement **withheld**. This is a reversible diagnostic checkpoint, not a completed P5 gate.

- All 50 runtime files named by the inventory exist in root molvid/; no src/ or molvid/models/ package was made. tests/test_imports.py loads every new module from a process outside the repository and finds no old data/module/trainer/evaluation/utils/scripts root imports or PVB public class aliases. Packaging discovery includes only molvid.*. The one historical trainer.codec_trainer.PVBCodecModel string in molvid/checkpoints.py is an exact old-checkpoint contract discriminator, not a public alias. README now identifies the current entrypoints and labels the paper-era commands as archive.
- Read-only legacy reachability found 130 tracked files under old data/module/trainer/evaluation/utils/scripts roots. Current train.py, infer_*.py, eval_codec.py, train_codec.py and Round 4/other experiment runners still import them; historical tests and YAML select them too. No old file was deleted or runner switched. User-provided design artifacts remain untracked and untouched.
- Real resources were present: approved R4 codec SHA ba10c44189cca837430abbd64afce2109a0daf0bda4f05971e0441abb2a5e6df, historical DiT SHA 808eb51e5c597629ebbd7270697af91affbc82a812cadd165eae984cba900240, CUDA GPU 5, and frozen train/valid manifest. The live manifest data hash exactly matched the DiT checkpoint. A 16-frame, 910-atom real validation clip was loaded; sealed test payload was not opened.
- The new real-clip zero-tolerance gate in tests/test_real_generation.py **fails** under CUBLAS_WORKSPACE_CONFIG=:4096:8, deterministic CUDA and TF32 disabled. Initial old/new final coordinates differed at most 7.62939453125e-06 Å. The first CPU/GPU block-center reduction mismatch (2.86102294921875e-06 Å) was corrected in molvid/generation.py by recomputing centers on the model device after CPU topology registration; existing generation tests still pass. Full output still differed. Stage checks then found exact old/new centered coordinates, graph edges/weights, frame features, Haar state input, and temporal linear weights/bias/strides, but separately allocated CUDA nn.Linear instances produced outputs differing by up to 2.5033950805664062e-06. Repeating the same old instance was exact. The failing assertion remains in the test; no tolerance was widened and no result is marked passed.
- In enter-container / torch-ito, the retained suite excluding that known-failing real gate passed 109 tests in 139.85 s after the generation change; git diff --check passed. The real gate separately failed. Short CUDA timing for the gate was about 7–10 s to its first mismatch; stage-wise throughput/memory profiling is not_run, so no performance conclusion is claimed.
- Still not_run: old-runner end-to-end H4/H8 real-system rollout and metric comparison; historical training-data/sampler continuation; full raw ATLAS/MISATO preprocessing; target inspect/migrate tools, benchmark scripts and experiment protocol relocation. These, plus the failing exact real generation gate and active old callers, prevent deletion or production cutover. Next: diagnose CUDA GEMM kernel/alignment semantics without weakening the numerical contract, then rerun full real generation/rollout and resume comparisons before retiring any path. No long training, dependency upgrades or remote push occurred.

## P5 follow-up — user-approved bounded CUDA parity (2026-09-18)

Status: the real H8 full-weight numerical gate now passes under explicit bounds; this supersedes only the zero-tolerance blocker above. Overall P5 cutover and legacy retirement remain withheld.

- The user accepted the measured 7.62939453125e-06 Å final-coordinate difference and removed the zero-tolerance requirement for this comparison. `tests/test_real_generation.py` now requires absolute error ≤1e-5 Å for generated coordinates, ≤1e-4 for intermediate codec latent fields (one observed difference was 4.57763671875e-05 in latent units), and ≤1e-5 for the isolated CUDA linear output; relative tolerance is zero throughout. The same-input records, graph, frame features, Haar input, mapped weights/bias and observed prefix still require exact equality. No failed test was removed or replaced.
- In `enter-container` / `torch-ito` with `CUBLAS_WORKSPACE_CONFIG=:4096:8 CUDA_VISIBLE_DEVICES=5 PYTHONDONTWRITEBYTECODE=1`, `python -m pytest -q --tb=short --color=no -p no:cacheprovider tests/test_real_generation.py` passed 1 test in 18.08 s on the real 16-frame, 910-atom validation clip and approved codec/DiT checkpoint pair. The sealed test payload remained unopened.
- The first 20-module retained-suite attempt exposed a separate PyTorch CUDA advanced-index implicit-broadcast internal assertion in the new reference DiT backend (2 failures, 108 passes). Explicit block-feature expansion in `molvid/dit/backend.py` preserves the reference computation; the triggering 7-module prefix then passed 47 tests. The full retained suite (`tests/test_cli.py test_codec.py test_conditioning.py test_data.py test_data_preprocess.py test_data_sampling.py test_dit.py test_evaluation.py test_flow.py test_generation.py test_geometry.py test_imports.py test_latent.py test_losses.py test_manifest.py test_metrics.py test_migration.py test_real_generation.py test_runtime_config.py test_training.py`, all under `tests/`) passed **110 tests in 152.85 s** with the same environment. `git diff --check` passed.
- Still `not_run`: independent old-runner H4/H8 real-system rollout and metric comparison, historical data/sampler continuation, full raw ATLAS/MISATO preprocessing, stage-wise performance profile, and planned inspection/migration/benchmark/protocol tools. The active old entrypoints remain untouched; no old source file is safe to retire yet. Next: implement remaining P4d tools in small batches, compare old-runner H4/H8 rollout and metrics, then re-audit callers before any switch or deletion. No long training, dependency upgrades or remote push.

## P4d6 checkpoint — read-only dataset inspection (2026-09-18)

Status: `tools/inspect_dataset.py` is usable for a verified frozen manifest. It opens train/valid only, streams records for system/trajectory/clip counts, atom/frame ranges and physical ps interval ranges, and labels `--max-clips` output as partial; it does not open the sealed test store.

- `enter-container` / `torch-ito`: `PYTHONDONTWRITEBYTECODE=1 python -m pytest -q --tb=short --color=no -p no:cacheprovider tests/test_inspect_dataset.py tests/test_manifest.py tests/test_imports.py` → 6 passed. `python -m tools.inspect_dataset --manifest-root /workspace/PVB/outputs/state_detail_codec_v2/t1/manifest_20260904_token80000 --max-clips 1` succeeded: train 8,928 available clips, valid 1,488, one scanned per split, 100 ps interval; both marked partial. The test payload stayed sealed.
- Next: model inspection, explicit artifact migration/export, short profile and experiment protocol tools. Full dataset scan was `not_run`; no performance claim, old entrypoint switch or deletion.

## P4d7 checkpoint — real model inspection (2026-09-18)

Status: `tools/inspect_model.py` reports the actual module tree, per-module owned parameter counts, frozen names/counts and one validation forward shape. The DiT probe constructs a repeated-observed-prefix scaffold; it does not supply true future coordinates as a condition.

- In `enter-container` / `torch-ito` with deterministic CUDA, `python -m pytest -q --tb=short --color=no -p no:cacheprovider tests/test_inspect_model.py` → 2 passed (including the real approved codec and migrated full-weight historical DiT on a 910-atom validation clip). A direct `python -m tools.inspect_model --kind codec` run on the approved checkpoint and validation index 0 succeeded: 856,136 parameters; 16×1,199×3 coordinate/reconstruction shapes and 4×1,199×128 scalar latent shape. The sealed test payload stayed unopened.
- Still `not_run`: the `--kind dit` CLI branch against a persistent new-format DiT checkpoint, full dataset scan, artifact export, short profile and protocol tools. Next: implement and verify explicit artifact migration, then benchmarks/protocol; do not switch old runners or delete source files on the basis of inspection alone.

## P5c/d diagnostic checkpoint — parent sampler and expanded generation (2026-09-18)

Status: parent-run sampler continuation is exact; expanded real generation is **not** accepted at the current coordinate bound. No old entrypoint switch or file retirement is authorized.

- The historical sequential baseline points to the capacity C48 parent checkpoint (SHA verified). Its inherited cursor is epoch 2/batch 1336 and its schedule hash uses the capacity-run `pvb.dit.capacity_data.schedule.v1` record, not the later sequential-run hash formula. On the actual parent `train48` store, old and new `TrajectoryCappedBatchSampler` state, full epoch-2 batches, stored schedule hash and the next three cursor batches matched exactly. `tests/test_real_sampler_resume.py` passed 1 test in `enter-container` / `torch-ito`. The parent data hash is `a9bdf4d8cfd0d4ea612bb5bc9a5aad29fdba1aaec800ce272c5fc5401217e1f6`, while the later baseline artifact config names `9daaf83fe5ee862634f7d1d3530e730adb4a8529ed37a2bc2fd330304ebfe184`; therefore this does **not** prove a resumable optimizer/data step on the later manifest. The capacity manifest uses `train48`/`train192`, so the current train/valid-only manifest loader was not used for that separate historical format; both old/new clip stores were opened read-only and no test store was opened.
- The extended real-weight CUDA check computes old/new H4 generation and an autoregressive two-segment H8 rollout on the same real 910-atom validation clip. With the previously approved 1e-5 Å absolute coordinate bound, H4 failed at 2.09808349609375e-05 Å (4/43,680 elements) and the two-segment rollout failed at 1.1444091796875e-05 Å (5/65,520 elements). Both attempts used `CUBLAS_WORKSPACE_CONFIG=:4096:8 CUDA_VISIBLE_DEVICES=5 PYTHONDONTWRITEBYTECODE=1 python -m pytest -q --tb=short --color=no -p no:cacheprovider tests/test_real_generation.py` in `enter-container` / `torch-ito`. The earlier single-H8 110-pass checkpoint remains valid, but the expanded test at this checkpoint is failing; no tolerance was raised and no failed assertion was removed. User direction on a distinct H4/rollout bound has been requested.
- Still `not_run`: old-runner metric parity for the expanded paths, direct historical optimizer/data continuation on the later manifest, artifact export, profiling and remaining protocol/benchmark tools. Next: apply only an explicitly accepted bounded threshold (if approved), rerun the full retained suite, then continue P4d tools and P5 reachability checks. No deletion, long training, dependency upgrade or remote push.

## P4d8/P5 short CUDA stage profile (2026-09-18)

Status: the planned short new-module profile is runnable; no old/new throughput or slowdown verdict is claimed.

- `benchmarks/profile.py` loaded SHA-verified historical codec/DiT weights into current modules and profiled a real 1,199-atom validation clip on GPU 5, with future coordinates erased from the DiT condition. One cold pass reported graph 80.7 ms / 60.9 MiB peak additional allocation, encoder 508.7 ms / 18,063.6 MiB, inclusive codec encode 404.4 ms / 18,064.8 MiB, decoder 10.4 ms / 167.3 MiB, DiT forward 57.6 ms / 181.8 MiB, and DiT forward+backward 121.9 ms / 1,639.5 MiB. These stages overlap in scope; the numbers must not be summed. No optimizer update occurred, DiT gradients were nonzero, and the frozen codec had no gradients.
- In `enter-container` / `torch-ito`, `CUBLAS_WORKSPACE_CONFIG=:4096:8 CUDA_VISIBLE_DEVICES=5 PYTHONDONTWRITEBYTECODE=1 python -m pytest -q --tb=short --color=no -p no:cacheprovider tests/test_profile.py tests/test_inspect_model.py tests/test_inspect_dataset.py tests/test_real_sampler_resume.py tests/test_imports.py` → 8 passed in 14.33 s. Direct `python -m benchmarks.profile` with the approved codec/DiT SHAs and frozen manifest also succeeded. The test split was not opened.
- Still `not_run`: matched old-runner stage profile (so material slowdown cannot be assessed), multi-run warm timing, overfit benchmark, artifact migration/export and experiment protocol tool. Next: complete those tools in small batches while the separate H4/rollout numerical bound awaits user direction.

## P5d accepted extended generation and obsolete-test audit (2026-09-18)

Status: the expanded real-weight CUDA numerical gate is accepted at its separate user-approved absolute coordinate bound; no old production entrypoint has been switched or retired.

- H4 and two-segment H8 old/new generation now use `rtol=0, atol=3e-5 Å`; the original single-H8 coordinate gate remains `rtol=0, atol=1e-5 Å`, and its latent/weight and exact observed-prefix checks are unchanged. The measured maxima were 2.09808349609375e-05 Å (H4) and 1.1444091796875e-05 Å (two-segment H8). The dedicated real-weight test passed 1/1; the 24-module retained suite passed **116/116** in `enter-container` / `torch-ito` with deterministic CUDA on GPU 5.
- Audited old tests against the flat package and runner/config filename references. Removed only `tests/test_coordinate_decoder.py`, `tests/test_temporal_codec.py`, `tests/test_visnet_spatial.py`, and `tests/test_visnet_spatial_v2.py`: they target retired legacy temporal/anchor or ViSNet experiment implementations, have no flat-package coverage role or active runner filename dependency, and remain recoverable in Git history. This is scope retirement, not deletion of failing assertions. The retained codec, geometry, conditioning, generation, optimizer/resume and CUDA parity tests remain green. `test_luna_review_contracts.py` and the round CUDA files are retained for now: they still cover old contracts or are named by historical runners; revisit only with their entrypoint retirement.
- Still `not_run`: direct old-runner H4/H8 metric parity, later-manifest historical optimizer/data continuation, full raw preprocessing, warm matched performance comparison, artifact export, overfit benchmark and protocol relocation. Next: complete the remaining P4d tools and resolve the old-runner/caller gates before production-path retirement. No long training, dependency upgrade or push.
## User-directed test and result archive (2026-09-18)

Status: the previously verified flat-package core suite is retained; historical experiment tests and result files are organized for reading. This batch did not rerun pytest, by user request, and did not switch production entrypoints.

- This intermediate batch initially archived legacy/parity tests while retaining flat-package coverage. The archive was subsequently purged from all Git history on 2026-09-20. The 116-pass result predates both operations and must not be represented as a fresh pass of the current tree.
- `tools/archive_results.py` ran through `enter-container` / `torch-ito`. `results_archive/README.md` indexes 15 experiment families; `KEY_RESULTS.md` gives source-linked time/data/model/training/result summaries, separating quick from final evaluations. Training histories are plotted; one-off checkpoint evaluations remain numeric in `evaluation_metrics.csv` (4,192 scalar rows with scope). A pilot summary says `completed_steps=4100` while its last validation row is at step 4500; the curated report flags this conflict rather than resolving it by guesswork.
- The archive packages 388 JSON/JSONL/CSV/TSV/Markdown/YAML/original image files as semantically renamed members of `raw_results.tar.gz`, with original→member names and SHA-256 in `raw_manifest.csv`. It carries 43 original figures and 55 newly drawn loss curves. Every tar member was reopened and hash-checked against the source. `outputs/` was not changed; checkpoints, logs, clip stores and sealed test data were excluded. Run-path dates are labeled as inferred, not file creation timestamps.
- Remaining P4d/P5 production cutover and historical optimizer/data continuation are still unverified. The user has explicitly stopped further test migration and numerical testing for this archive task. Do not imply that the archived legacy runners or full production switch passed a new gate.
